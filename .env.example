# ==========================================
# Agent-Commerce-OS MCP Server Environment
# ==========================================

# Required: Polar.sh API Key for metered billing ($0.10 / request)
POLAR_API_KEY=your_polar_api_key_here

# Optional: API Gateway URL (Layer A) defaults to https://api.sakutto.works/v1/normalize_web_data
GATEWAY_URL=https://api.sakutto.works/v1/normalize_web_data